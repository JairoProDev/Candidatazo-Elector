"use client";

import { FeedShell } from "@/components/feed/FeedShell";

export default function FeedOcean2Page() {
  // Placeholder: módulo derivado reusando el mismo motor/UX del feed.
  // Se reemplazará por un ranking y catálogo específico cuando exista dataset/API.
  return <FeedShell />;
}

